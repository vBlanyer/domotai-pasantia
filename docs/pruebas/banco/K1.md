# K1 · middleware comprometido contra core-db: aprobado; cascada no avisada (fallo conocido K1)

- nivel: vivo

## Atacar (desde middleware)
```bash
docker exec clab-banco-middleware sh -c 'for i in $(seq 1 10); do sshpass -p mal_$i ssh -o StrictHostKeyChecking=no -o ConnectTimeout=4 -o PubkeyAuthentication=no -o PreferredAuthentications=password cliente@10.50.0.10 id >/dev/null 2>&1; done; true'
```

## Esperado
```json
{
 "requiere_humano": true,
 "veredicto": "aprobar",
 "preguntas": 1,
 "prediccion_cascada": [],
 "regla": [
  "core-db",
  "-A INPUT -s 10.40.0.10/32 -j DROP"
 ],
 "caen": [
  "api-movil",
  "atm",
  "middleware",
  "web-banking"
 ]
}
```

## Deshacer
```bash
docker exec clab-banco-core-db iptables -D INPUT -s 10.40.0.10 -j DROP
```
